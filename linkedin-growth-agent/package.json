{
  "name": "linkedin-growth-agent",
  "version": "0.1.0",
  "description": "LemonCake LinkedIn outreach agent — enriches leads and drafts Base44-style DMs into Notion",
  "main": "dist/agent.js",
  "scripts": {
    "build": "tsc",
    "agent": "tsc && node dist/agent.js",
    "agent:dry": "tsc && DRY_RUN=1 node dist/agent.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.4.5"
  }
}
