# TODO — 2026-04-16

---

## ✅ 実装済み（本セッションで完了）

- [x] APIプロキシルート (`ANY /api/proxy/:serviceId/*`)
- [x] freee OAuth フロー + 401自動トークンリフレッシュ
- [x] DirectoryPage を API fetch に切り替え
- [x] 日本/グローバルサービス群を seed に追加（gBizINFO, e-Gov, Abstract, IPinfo, OXR, Hunter.io, Jina, Firecrawl）
- [x] Jina Reader プロキシ修正（`?url=` クエリパラメータ方式）
- [x] **E2E Workflow ステートマシン実装**
  - `Workflow` モデル + `WorkflowState` enum を DB に push
  - `api/src/lib/workflow-engine.ts` — 状態遷移・USDC 与信ロック
  - `api/src/workers/workflowStep.ts` — BullMQ ワーカー（全7ステップ）
  - `api/src/routes/workflows.ts` — `POST /api/workflows/e2e-procurement` 他
  - `api/src/routes/webhooks/cloudsign.ts` — 署名完了 Webhook
  - `api/src/routes/webhooks/aftership.ts` — 納品完了 Webhook
  - seed に CloudSign / TRUSTDOCK / Slack / Raksul / AfterShip を追加

---

## 優先度 HIGH（手動作業が必要）

### 🔑 1. 外部サービス API キーを取得 → Railway にセット → 再シード

| 環境変数名 | 取得元 | 用途 |
|-----------|--------|------|
| `GBIZINFO_API_KEY` | https://info.gbiz.go.jp/api/index.html | gBizINFO 法人情報 |
| `ABSTRACT_API_KEY` | https://app.abstractapi.com/api/vat/documentation | EU VAT 検証 |
| `IPINFO_TOKEN` | https://ipinfo.io/signup | IP リスク判定 |
| `OPENEXCHANGERATES_APP_ID` | https://openexchangerates.org/signup | 為替レート |
| `HUNTER_API_KEY` | https://hunter.io/api-keys | 企業メール検索 |
| `FIRECRAWL_API_KEY` | https://www.firecrawl.dev/app/api-keys | Web スクレイピング |
| `JINA_API_KEY` | https://jina.ai/api-dashboard | LLM Web Reader |
| `CLOUDSIGN_API_KEY` | https://app.cloudsign.jp/settings/api | 電子契約 |
| `CLOUDSIGN_WEBHOOK_SECRET` | CloudSign ダッシュボード → Webhook 設定 | 署名検証 |
| `AFTERSHIP_API_KEY` | https://app.aftership.com/settings/api | 物流追跡 |
| `AFTERSHIP_WEBHOOK_SECRET` | AfterShip ダッシュボード → Webhook 設定 | 署名検証 |
| `SLACK_BOT_TOKEN` | https://api.slack.com/apps → Bot Token | Human-in-the-loop |
| `RAKSUL_API_KEY` | https://raksul.com/api/ → API 利用申請 | 印刷・DM 発注 |
| `TRUSTDOCK_API_KEY` | https://trustdock.io → 営業問い合わせ | eKYC |

```bash
# Railway でセット後
cd api && npx tsx prisma/seed.ts
```

---

### 🌐 2. Webhook URL を各サービスのダッシュボードに登録（手動）

| サービス | 登録先 | Webhook URL |
|---------|--------|-------------|
| CloudSign | https://app.cloudsign.jp/settings/webhooks | `https://skillful-blessing-production.up.railway.app/api/webhooks/cloudsign` |
| AfterShip | https://app.aftership.com/settings/webhook | `https://skillful-blessing-production.up.railway.app/api/webhooks/aftership` |
| Stripe | https://dashboard.stripe.com/webhooks | 既存 Webhook に `checkout.session.completed` を追加 |
| GitHub (Recipe) | リポジトリ Settings → Webhooks | `https://skillful-blessing-production.up.railway.app/api/webhooks/github` |

---

### 🏛️ 3. 国税庁 インボイス照合 API — アプリ登録 + コード修正（4/17 以降）

1. https://www.invoice-kohyo.nta.go.jp/index.html でアプリ登録 → `appId` 取得
2. Railway に `NTA_APP_ID` をセット
3. `api/src/lib/tax.ts:35` の URL を修正:
   ```typescript
   // 旧（廃止済み・現在 404）:
   const NTA_API_BASE = "https://web-api.invoice-kohyo.nta.go.jp/api/v1";
   // 新:
   const NTA_API_BASE = "https://web-api.invoice-kohyo.nta.go.jp/api/1";
   ```
4. `checkInvoiceRegistration()` のリクエストを新エンドポイント形式に変更:
   `GET /api/1/sealed/v01/matching?id=<appId>&number=T1234567890123`

---

### 💳 4. Stripe Webhook に `checkout.session.completed` を追加 + E2E テスト

1. https://dashboard.stripe.com/webhooks → 既存エンドポイント → イベント追加
2. E2E テスト: `POST /api/payments/checkout` → 振込完了 → Buyer 残高確認

---

### 🧾 5. freee 仕訳の自動トリガー実装（未接続）

`createFreeeTransaction()` が `api/src/lib/freee.ts` に実装済みだが**どこからも呼ばれていない**。
Workflow の BOOKKEEPING ステップには組み込み済みだが、通常の Charge（プロキシ経由の1回払い）には未接続。

`api/src/workers/usdcTransfer.ts` の COMPLETED 更新後（`job.updateProgress(100)` の前）に追加:

```typescript
// freee 仕訳（失敗しても送金は完了しているので catch で握りつぶす）
try {
  const { createFreeeTransaction } = await import("../lib/freee.js");
  await createFreeeTransaction({
    issueDate:         new Date().toISOString().slice(0, 10),
    description:       `${service.name} API利用料`,
    amountUsdc:        amountUsdc,
    amountJpy:         Math.round(Number(amountUsdc) * Number(process.env.JPY_USDC_RATE ?? "150")),
    providerName:      service.provider.name,
    invoiceRegistered: false,  // TODO: 国税庁 API が復活したら実照合に変更
  });
} catch (freeeErr) {
  console.error("[Worker] freee 仕訳作成失敗:", freeeErr);
}
```

---

## 優先度 MEDIUM

### ① GitHub Recipe — Webhook エンドポイント実装

**ファイル:** `api/src/routes/webhooks/github.ts`（新規作成）

```typescript
// POST /api/webhooks/github
// X-Hub-Signature-256 で署名検証
// event: pull_request + action: closed + merged: true をフィルタ
// → コントリビューター location 判定 → 源泉徴収計算 → freee 仕訳
```

- `GITHUB_WEBHOOK_SECRET` → Railway にセット
- GitHub リポジトリ → Settings → Webhooks に URL 登録

---

### ② KYB Recipe — `api/src/lib/kyb.ts` 実装

```typescript
export async function runKybCheck(corporateNumber: string): Promise<{
  approved: boolean; riskScore: number; escalate: boolean; details: object;
}>
```

リスクスコアリング:
| 条件 | スコア |
|------|--------|
| 設立 < 1年 | +30 |
| 資本金 < 100万円 | +20 |
| gBizINFO 未登録 | +40 |
| 国税庁 非適格 | +20 |

`POST /api/transfers` のミドルウェアとして挿入。スコア ≥ 90 で Resend API により管理者通知。

---

### ③ docs/platform-overview-for-ai-agents.md 再作成

セッション中に作成したが消えた。Recipes 4件（GitHub / Physical / Cloud / KYB）と新サービス一覧（CloudSign, TRUSTDOCK, Raksul, AfterShip）を含む完全版を再作成。

---

## 優先度 LOW（Phase 2 / 本番化）

### ⑥ SKIP_WORKER=false + ホットウォレット設定

現在 `SKIP_WORKER=true`・`HOT_WALLET_PRIVATE_KEY="dummy_key_for_now"` でオンチェーン送金が無効。

1. MetaMask で Polygon 専用ホットウォレット作成
2. Railway に `HOT_WALLET_PRIVATE_KEY` をセット
3. ウォレットに MATIC（ガス代用）を入金
4. Railway に `SKIP_WORKER=false` をセット
5. Polygon RPC を Alchemy に切り替え:
   ```
   POLYGON_RPC_URL=https://polygon-mainnet.g.alchemy.com/v2/<YOUR_KEY>
   ```

### ⑦ Stripe 本番キー切り替え

現在 `sk_test_xxx` のままテストモード。本番化時に切り替え。

### ⑧ Raksul / TRUSTDOCK API 申請

- ラクスル: https://raksul.com/api/ でAPI利用申請（代理店経由の場合あり）
- TRUSTDOCK: 営業問い合わせが必要（エンタープライズ向け）

---

## チェックリスト

- [ ] Railway に 14 個の API キーをセット → 再シード
- [ ] CloudSign Webhook URL 登録 + `CLOUDSIGN_WEBHOOK_SECRET` セット
- [ ] AfterShip Webhook URL 登録 + `AFTERSHIP_WEBHOOK_SECRET` セット
- [ ] Stripe Webhook に `checkout.session.completed` 追加 + E2E テスト
- [ ] 国税庁 appId 取得 + `tax.ts` URL 修正（4/17 以降）
- [ ] `usdcTransfer.ts` に freee 仕訳自動呼び出しを追加
- [ ] GitHub Webhook エンドポイント実装
- [ ] KYB チェック関数 (`lib/kyb.ts`) 実装
- [ ] platform-overview-for-ai-agents.md 再作成
- [ ] ホットウォレット設定 + SKIP_WORKER=false（Phase 2）
- [ ] Stripe 本番キー切り替え（Phase 2）
- [ ] ラクスル / TRUSTDOCK API 申請（Phase 2）
